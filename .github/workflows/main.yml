name: Main
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - uses: ./
        with:
          test: true
          # gh-token: ''

